# Use the official Nginx image from Docker Hub
FROM nginx:1.21-alpine

# Copy all the frontend files into the Nginx public directory
COPY . /usr/share/nginx/html

# The container will listen on this port
EXPOSE 80